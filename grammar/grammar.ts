var x = 1;

var y;

var firstName = 'Brian';

var num1 = 100;
var num2: number = 200;

function addNumbers(n1: number, n2: number, n3: number) {
    var result = n1 + n2 + n3;
    var msg = `Sum is ${result}`;
    console.log(msg);

}

addNumbers(num1, num2, 8);